{
  "name": "host2port-reverse-proxy-server",
  "version": "1.0.0",
  "description": "host2port reverse proxy server",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "build": "esbuild --bundle --minify --platform=node --outfile=dist/index.js src/index.ts"
  },
  "keywords": [],
  "author": "lnkkerst",
  "license": "MIT",
  "dependencies": {
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "@types/http-proxy": "^1.17.10",
    "esbuild": "^0.17.11",
    "ts-node-dev": "^2.0.0"
  }
}
